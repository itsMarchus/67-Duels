# Local Meme Assets

Drop meme images in this folder and list their filenames in `manifest.json`, for example:

```json
["sixty-seven.png", "duel-face.jpg"]
```

The app loads these files locally from `/memes/<filename>`. If the manifest is empty, it uses text stickers instead.
